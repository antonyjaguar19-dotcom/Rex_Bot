# ============================================================
#  Claw Bot Agent - Python dependencies
# ============================================================
#
#  Install everything with:
#      pip install -r requirements.txt
#
#  Pins match the working venv (re-pinned 2026-06-10). The FULL
#  environment snapshot (every transitive package) lives in
#  requirements.lock.txt — use that to rebuild the venv exactly:
#      pip install -r requirements.lock.txt
#
#  NOT in this file (installed separately):
#  - ComfyUI: its own portable install in 01_ComfyUI/
#  - PyTorch + CUDA: install for your GPU first. RTX 5080 uses:
#       pip install torch --index-url https://download.pytorch.org/whl/cu128
#  - Ollama: native install (the bot talks to it over HTTP;
#    the `ollama` pip package is NOT used)
#  - Kokoro TTS + audiocraft (MusicGen): optional audio extras,
#    see requirements.lock.txt for the working versions
# ============================================================

# --- Discord bot framework ---------------------------------
discord.py==2.7.1

# --- HTTP clients (ComfyUI + Ollama APIs) -------------------
aiohttp==3.13.5
requests==2.33.1

# --- Configuration & environment ---------------------------
python-dotenv==1.2.2

# --- Image handling (storyboards, contact sheets) ----------
Pillow==12.2.0

# --- Async + scheduling ------------------------------------
APScheduler==3.11.2        # daily auto-story trigger
aiofiles==25.1.0

# --- Config validation --------------------------------------
jsonschema==4.26.0

# --- Dashboards ---------------------------------------------
nicegui==3.12.1            # ACTIVE dashboard (localhost:7860)
gradio==6.15.2             # legacy fallback dashboard

# --- Tests ---------------------------------------------------
pytest==9.0.3
