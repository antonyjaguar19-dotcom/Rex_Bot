# ============================================================
#  Claw Bot Agent - Python dependencies
# ============================================================
#
#  Install everything with:
#      pip install -r requirements.txt
#
#  NOTE on what's NOT in this file:
#  --------------------------------
#  - ComfyUI itself: cloned separately into 01_Tools/ComfyUI
#    (it has its own requirements; install via its own setup)
#  - PyTorch + CUDA: install manually for your GPU first.
#    For RTX 3070 + Windows + CUDA 12.1:
#       pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#  - Ollama: separate native install from ollama.com
#
#  Pinned versions tested as of Phase 4 + Session 1A.
# ============================================================


# --- Discord bot framework ---------------------------------
discord.py==2.3.2          # Bot client, slash commands, reactions

# --- HTTP client for ComfyUI API ---------------------------
aiohttp==3.9.5             # Async HTTP for /prompt + /history polling
requests==2.32.3           # Sync HTTP for simple status checks

# --- Configuration & environment ---------------------------
python-dotenv==1.0.1       # Loads .env file (Discord token, secrets)

# --- Ollama client (local LLM) -----------------------------
ollama==0.3.3              # Python wrapper around local Ollama server

# --- Image handling (storyboards, thumbnails) --------------
Pillow==10.4.0             # Open, resize, convert PNG/JPG outputs

# --- Logging & file utilities ------------------------------
loguru==0.7.2              # Cleaner logging than stdlib (writes to 06_Logs/)
watchdog==4.0.1            # Watch ComfyUI output folder for new files

# --- GPU monitoring (for health dashboard) -----------------
nvidia-ml-py==12.555.43    # VRAM usage stats from RTX 3070

# --- Async + scheduling ------------------------------------
APScheduler==3.10.4        # Daily script generation triggers
aiofiles==24.1.0           # Async file I/O for non-blocking saves

# --- JSON schema validation (for models.json, styles.json) -
jsonschema==4.23.0         # Validates config files on bot startup


# ============================================================
#  Coming for Phase 5 (video) - DO NOT install yet
# ============================================================
# moviepy            # Video assembly (Phase 6)
# ffmpeg-python      # ffmpeg bindings (Phase 6)
# google-api-python-client    # YouTube upload (Phase 6)
# google-auth-oauthlib        # YouTube OAuth (Phase 6)

gradio>=5.0
nicegui>=3.0
